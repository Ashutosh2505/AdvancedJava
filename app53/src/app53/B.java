package app53;

public class B {
	public static void main(String[] args) {
		Integer obj = Integer.valueOf(100);
		int i = obj.intValue();
		System.out.println("done");
	}

}
//Autoboxing & Unboxing:

//Autoboxing: Converting a primitive type to its corresponding wrapper class object (e.g., int → Integer).
//Unboxing: Converting a wrapper class object to its corresponding primitive type (e.g., Integer → int).